<%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, :role => 'form', class: "margin-bottom-0" }) do |f| %>
  <%= render "shared/form_error_messages", f: f%>
  <div class="form-group m-b-20">
      <%= f.email_field :email, autofocus: true , placeholder:"Email Address", class:"form-control input-lg", required:true  %>
  </div>

  <div class="login-buttons">
    <%= html_submit(content=fa_icon('paper-plane', :text => "Resend confirmation instructions"), extra={class: "btn btn-success btn-block btn-lg"}) %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
