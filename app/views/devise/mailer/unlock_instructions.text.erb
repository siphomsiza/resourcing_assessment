Hello <%= @resource.email %>!

Your account has been locked due to an excessive number of unsuccessful sign in attempts  on <%= Settings.APP_NAME %>(<%=root_url %>).

Click the link below to unlock your account:

<%= unlock_url(@resource, unlock_token: @token) %>
