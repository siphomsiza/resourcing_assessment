Welcome <%= @email %>!

Your account has been registered on <%= Settings.APP_NAME %>(<%=root_url %>)

You can confirm your account email through the link below:

<%= confirmation_url(@resource, confirmation_token: @token) %>
